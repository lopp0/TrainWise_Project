# Lessons

Format: `[YYYY-MM-DD] | what went wrong | rule to follow next time`

[2026-04-21] | wrote ACWR boundaries inclusively on the wrong side (Green ≤0.8 / Red ≥1.3) | thresholds per doc + user clarification: Green ratio<0.8, Yellow 0.8≤ratio≤1.3, Red ratio>1.3 — use strict < for Green and strict > for Red
[2026-04-21] | kept intensityFactor in load formula | intensityFactor has been fully removed from the project (DB, backend, frontend) — session load = duration × exertion only
[2026-04-21] | edit-workout path in StatsScreen didn't send calculatedLoadForSession and didn't call calculateDailyLoad after save, so Warnings kept stale Red/high AC ratio | any screen that creates/updates an ActivityLog MUST set calculatedLoadForSession = duration × exertion AND call calculateDailyLoad(userId) afterwards — otherwise the DailyLoad row on the server won't reflect the edit
[2026-04-21] | calling calculateDailyLoad() only recalcs ONE day (default today) — editing an older session leaves that older day's DailyLoad row stale | when editing a past session, recalc BOTH the edited day AND today so the rolling 7/28-day windows pick up the change: calculateDailyLoad(userId, editedDate) then calculateDailyLoad(userId, today)
[2026-04-21] | fetched static recommendations from backend; same "take a break" showed for Green/Yellow/Red | backend has no recommendation generator (RecommendationBL only has Create/GetByUser) — the Warnings screen must build the text client-side from loadLevel + acRatio, don't rely on getRecommendationsByUser
[2026-04-21] | weekly chart kept showing high bars after user edited sessions because DailyLoad rows persist their AcuteLoad value from when they were saved (rolling-sum snapshot) — editing ActivityLogs doesn't retroactively update past DailyLoad rows | Refresh on Warnings must recalc ALL 7 days of DailyLoad (loop i=6..0, call calculateDailyLoad(userId, day)), not just today — stale DailyLoad rows are the #1 cause of "status won't change after edits"
[2026-04-21] | Warnings chart sourced bars from DailyLoad.acuteLoad (7-day rolling sum ending on that day) which leaked previous-week sessions into this-week bars | weekly bar chart must aggregate ActivityLogs per-day within the selected week's Sun-Sat window — do NOT use rolling-sum fields for visual per-day bars
[2026-04-21] | Warnings status/AC ratio was pulled from backend DailyLoad rows, which are per-day snapshots that include prior-week sessions in their acute window — status stayed Red for every viewed week even when the bars showed tiny loads | status/AC ratio on the Warnings dashboard is computed client-side from ActivityLogs for the DISPLAYED week: acute = sum(loads in displayed Sun-Sat), chronic = sum(loads in prior 21 days)/3, ratio = acute/chronic. Never read DailyLoad.loadLevel for the per-week view
[2026-04-22] | jumped to soft-delete for ActivityLogs anticipating coach-history access; user later reversed because coach feature will reuse the live trainee dashboard, not a history table | when user mentions a "future feature" as the rationale for added complexity, ask how that future feature will actually work before adding the persistence/schema scaffolding — don't pre-build for assumed designs
[2026-04-22] | wrote `apiClient.post('/api/Users', userData)` in registerUser when the parameter was named `payload` — would have crashed with ReferenceError on signup submit | when you write a function that wraps another call, double-check the argument variable name matches the function signature, especially in axios POST/PUT bodies
[2026-04-22] | left duplicate `<Stack.Screen name="SignUp">` lines in NavigationStack.js after manual edit, breaking the AuthStack with "duplicate screen named" render error | React Navigation's `Stack.Navigator` does not allow two `Screen` children with the same `name` — when adding screens always grep for the existing name first
[2026-04-22] | HomeScreen weekly chart drifted from WarningsDashboardScreen — different aggregation, color thresholds, refresh strategy → bars showed wrong values & wrong colors | when two screens visualize the same data (e.g. weekly load), keep aggregation + color thresholds in a single shared module — don't duplicate getBarColor in both files
[2026-04-22] | StatsScreen ZoomedBarChart hardcoded selected bar to pink (#ff2d6f) regardless of load value | for selection emphasis use opacity/size/border, not a fixed selection color — that destroys the chart's color encoding
[2026-04-22] | HomeScreen used useEffect with [loadData] dependency, never re-fetched after user came back from another tab — deletes on the Health tab didn't propagate | for tab-switched data screens, prefer `useFocusEffect(useCallback(loadFn, deps))` over plain useEffect so the data refreshes when the user returns to the tab
[2026-04-22] | TrainWiseExpo/.gitignore had blanket `/android` → would lose manual MainActivity.kt + AndroidManifest.xml edits required for Health Connect | when ignoring auto-generated native folders, list the build subdirs explicitly (`/android/build/`, `/android/app/build/`, etc.) — never ignore the entire `/android` folder if there are tracked manual edits
[2026-04-26] | Mark Recovered button on ActiveInjuriesScreen is wired end-to-end (PUT /injuryreport/{id}/recover → sp_MarkInjuryRecovered) and confirmed working — do not treat it as an open task | when re-reading project state, verify each "pending" item by tracing UI → service → controller → BL → DAL before listing it as open
[2026-04-26] | InjuryReportBL.Create validates Severity 1..5 while InjuryReportScreen slider posts 1..10 → submits with severity > 5 silently 400 | whenever you see a numeric range in a UI control, grep the BL/SP for the matching validator before assuming the range is consistent
[2026-04-26] | jumped to "missing IsDeleted filter in SP" as the root cause of recalc 500, but the actual schema never had IsDeleted — real bug was DailyLoadDAL.GetActivityLogsForLoad passing @UserID/@Date when sp_GetActivityLogsForLoad expects @UserID/@StartDate/@EndDate | always read the live SQL Script + the DAL parameter dictionary together; never trust historical comments about "what the SP used to filter on"
[2026-04-26] | Settings save sent only profile fields; backend UpdateUserRequest validates ActivityLevel/ExperienceLevel ∈ [1,3] and BadRequest'd because they were 0 by default | when a screen edits a partial subset of a server-side model that has `range` validators, fetch the full record first and echo unchanged fields back in the payload — never let nullable-with-default fields slip through as 0
[2026-04-26] | HC TotalCaloriesBurned was wired up but it includes BMR — short workouts read 1500-1800 kcal vs Samsung Fit's 200-300 kcal | for workout calories use ActiveCaloriesBurned first; Total only as fallback with a BMR subtraction (~70 kcal/hr) so the number stays sane
[2026-04-26] | HC delete loop: deleting a backend log that originated from HC re-imported it on the next auto-sync because HC still has the record and TrainWise can't write back to HC | Persist a tombstone set in AsyncStorage keyed by normalized startTime; SyncService.deduplicateWorkouts must consult it before posting. Manual-source workouts skip tombstoning (they don't exist in HC)
[2026-04-26] | WarningsDashboardScreen used "uncoupled" ACWR (chronic = prior 21 days / 3) — caused chronic baseline to swing wildly between adjacent weeks and made the displayed ratio misleading | Use coupled ACWR per Gabbett 2016: chronic = sum(28-day window ENDING at displayed weekEnd, INCLUDING the displayed week) / 4. Backend LoadCalculationBL already uses this — keep client + server formulas aligned
[2026-04-26] | HomeScreen.buildWeeklyData stored `dayIndex: i` (array position 0..6) and used DAYS[dayIndex] to label, which broke when weekStartDay != Sunday | dayIndex must be the JS day-of-week (date.getDay()), not the array position — only then does DAYS[dayIndex] map to the right label under any week-start setting
[2026-04-26] | shipped a "global Colors swap" theme system but several screens (HomeScreen, StatsScreen, ProfileScreen) had hardcoded hex literals in their styles — light theme appeared half-applied | the ThemeContext only swaps the `Colors` singleton; any screen using `'#xxxxxx'` literals stays dark forever. Before claiming a theme works, grep `'#[0-9a-fA-F]{3,8}'` across `src/screens/**` and `src/components/**` and convert each to `Colors.*` (except semantic traffic-light bar colors)
[2026-04-26] | SettingsScreen called the HTTP `updateUser` but never updated AuthContext, so HomeScreen's "Hello {name}" and ProfileScreen's info card kept showing the cached login snapshot | After any successful PUT /users/{id}, also call `useAuth().updateUser({...})` with the same field names AuthContext normalizes on login (fullName, email, height, weight, etc.) so the in-memory user object + AsyncStorage stay in sync. Rename the HTTP import (e.g. `updateUserApi`) to avoid shadowing the AuthContext method
[2026-04-26] | "global Colors swap" theme didn't apply because `StyleSheet.create({...Colors.x...})` at module level captures hex values at import time — mutating Colors later doesn't update existing stylesheets even with a Fragment-key remount (modules stay cached) | every themed screen MUST move styles inside the component via `useThemedStyles(makeStyles)` where `makeStyles = (Colors) => StyleSheet.create({...})`. The hook is useMemo-keyed on theme and re-runs the factory after applyTheme mutates Colors. Reading Colors.x in JSX/inline-function args still works because the object reference is stable
[2026-04-26] | UserDAL.UpdateUser only passed FullName/BirthYear/Gender/Height/Weight/ActivityLevel/DeviceType/ExperienceLevel — Email, UserName, HealthDeclaration, ConfirmTerms, TermConfirmationDate fell through to sp_UpdateUser's `= NULL` defaults and the SP wrote NULL into the columns on every save | when an SP has nullable-defaulted parameters, the DAL must pass EVERY editable column or the SP silently wipes them. Always grep the SP signature against the param dictionary
[2026-04-26] | UpdateUserRequest DTO was missing UserName + Email fields → SettingsScreen's email change deserialized as null at the controller, BL passed null to DAL, SP wrote NULL → email "deleted" on every save | when a controller does a partial update via DTO, the DTO must include EVERY editable field. Missing properties bind silently to default values (null for strings, 0 for ints) and become hard-to-trace data wipes
[2026-04-26] | Sign-up alert showed "[object Object]" because api.js did `throw new Error(error.response?.data || ...)` and ASP.NET returned a ValidationProblemDetails object (when CreateUserRequest's non-nullable int/byte fields received JSON null from the frontend) | when an axios error's response.data may be either a string or an object, branch on typeof: strings pass through as-is; objects need .errors/.title/.detail extraction or JSON.stringify. Never pass an object to `new Error()` — it stringifies as "[object Object]"
[2026-04-26] | SignUpFinal sent userName: null and birthYear: null to the backend; the BL rejected userName, and ASP.NET 400-failed model binding on int BirthYear because JSON null can't bind to non-nullable int | always provide safe defaults from the frontend for fields the backend declares as non-nullable primitives (e.g. derive userName from email prefix, default birthYear to a sane year). Alternatively make the DTO fields nullable (int? / byte?) — but defaulting at the source is simpler than chasing every binding edge case
[2026-04-26] | every cold app launch red-logged "Sync failed: ... Network Error" twice (once from SyncService, once from useSyncWorkouts) because backend often isn't running when the auto-sync fires | classify network errors as recoverable: pattern-match `/network|timeout|econn|fetch/i` and use console.warn instead of console.error. Reset the auto-sync throttle to 0 after a network failure and schedule a 4s opportunistic retry so the user doesn't have to wait 30s. Same treatment in HomeScreen.loadData / GoogleFitScreen.loadWorkouts / HealthSyncContext.refreshUnconfirmedCount — anything that re-runs on focus shouldn't red-banner on a transient outage
[2026-05-31] | DBservice.Connect() built its own IConfigurationRoot from `appsettings.json` only, so the Azure App Service Connection-strings blade was completely ignored — every DB call hit the baked-in `Lirone\SQLEXPRESS` even on Azure | when an app reads config, use `.AddEnvironmentVariables()` (and `.SetBasePath(AppContext.BaseDirectory)`) so platform-injected values (Azure `ConnectionStrings__*` / `SQLAZURECONNSTR_*` env vars) win over local JSON. Don't refactor every DAL to DI just to fix a connection string — env-var fallback is the smallest possible change
[2026-05-31] | shipped Azure-only Swagger (gated by `app.Environment.IsDevelopment()`) so `/swagger` 404'd on the production URL just when we needed it most for triage | for school/demo projects keep Swagger mounted in production — the security tradeoff is negligible compared to the debugging value of an always-on API explorer. Gate it only for real customer-facing prod
[2026-05-31] | `api/api.js` had `BASE_URL = '.../api'` AND every call written as `apiClient.post('/api/Users', ...)` → real URL became `/api/api/Users` → 404 on signup/login/devices | when `apiClient.create({ baseURL })` already includes a path segment, every endpoint call MUST start at the resource (`/Users`, not `/api/Users`). Cross-check by skimming the BASE_URL and the first call in the same file
[2026-05-31] | StatsScreen WeeklyBarChart and ZoomedBarChart were defined at MODULE scope but referenced `styles` (which only exists inside the StatsScreen function via `useThemedStyles`) → fatal `ReferenceError: Property 'styles' doesn't exist` crashed the Home dashboard | sub-components that need themed `styles` must either (a) be defined INSIDE the screen function so they close over `styles`, or (b) receive `styles` as a prop from the parent. Module-level components cannot reference component-scoped variables — this is not specific to React Native
[2026-05-31] | spent an hour fighting Health Connect crash thinking it was a config bug; root cause was MainActivity.kt missing `HealthConnectPermissionDelegate.setPermissionDelegate(this)` and AndroidManifest.xml missing the `<package>` query + `<activity-alias>` — both wiped by past EAS prebuild attempts | at the start of any HC-related session, FIRST verify the native files match CLAUDE.md's "Native HC requirements" checklist before debugging the React side. The `react-native-health-connect` plugin output alone is incomplete for Android 14+
[2026-05-31] | even after restoring MainActivity + manifest exactly per CLAUDE.md, HC still silently denied permissions on Android 16 — logcat showed `PermissionsActivity: App should support rationale intent, finishing!` from PID 6050 (google permission controller) | Android 14+/16 require `<category android:name="android.intent.category.DEFAULT"/>` on the `ACTION_SHOW_PERMISSIONS_RATIONALE` intent-filter — without it the system PermissionsActivity rejects the activity during discovery. Add DEFAULT on BOTH the MainActivity intent-filter AND the activity-alias
[2026-05-31] | EAS Build was attempted (commits 9e9ae62 / c229857 / 1e148d1) and regenerated `android/` from `app.json`+plugins, silently dropping all the manual HC native fixes that CLAUDE.md warns about — broke HC for weeks until logs pointed at the regression | EAS Build = `expo prebuild` server-side. Until the manual native edits (MainActivity delegate, manifest package query, activity-alias, DEFAULT category) are ported into a custom Expo config plugin, EAS Build is forbidden. Use `npx expo run:android --variant release` for production APKs
[2026-05-31] | user pasted their real OpenAI API key into `openai.js` as the "error message text" while attempting to set the env var — the key was now in source code, in the chat transcript, and would have been bundled into every future APK build | when a user is unsure how to configure an env var, give precise file path + exact one-line format BEFORE they improvise. Treat any secret that appears in a code edit or chat message as compromised: have the user revoke immediately, generate a new one, and restore the file. `EXPO_PUBLIC_*` vars are baked into the APK in plain text regardless — warn loudly
[2026-05-31] | error UI in AddWorkoutScreen showed only the generic "Could not save workout. Check your connection" alert even though the backend returned the actual SQL/validation message in `response.data` | when an axios call wraps a controller that does `return BadRequest(ex.Message)`, surface `error?.response?.data` in the user-visible alert during development. Hiding the server's diagnostic behind a generic string adds 30+ minutes per debugging round-trip
[2026-05-31] | InjuryReportScreen's TextInput sat below the keyboard with no `KeyboardAvoidingView`, so the doctor-notes textarea was invisible while typing on Android | any screen with a multiline TextInput (especially below the fold of a ScrollView) needs `<KeyboardAvoidingView behavior={Platform.OS === 'ios' ? 'padding' : 'height'}>` wrapping the ScrollView, plus `keyboardShouldPersistTaps="handled"` so users can dismiss the keyboard by tapping elsewhere
[2026-05-31] | almost told the user "teammate's HomeScreen is identical, nothing to merge" based on a visual diff of the two pasted snippets — would have skipped real divergence | when a teammate pastes a file claiming "changes", always Read the on-disk version BEFORE comparing. Teammates' copies are often weeks behind the working tree, so the real merge target is the live file, not the pasted "before"
[2026-05-31] | teammate's AIChatScreen source had a corrupted emoji (`ðª` — UTF-8 bytes of 💪 mis-decoded as Latin-1) in the welcome message and I almost copied it verbatim | when porting code from a teammate's paste, scan for suspicious 2-3 character glyph sequences (`ðª`, `â€™`, `ï»¿`) — those are always mojibake from a UTF-8/Latin-1 round-trip. Replace with the intended unicode (💪, ', etc.) before saving
[2026-05-31] | added `.env` to TrainWiseExpo without checking gitignore — only `.env*.local` was ignored, so a bare `.env` would have been committed with the API key inside | whenever a project adds an `.env` file, immediately verify `.gitignore` matches that exact pattern (not just `.env.local` / `.env*.local`). The leak happens at the first `git add .`, before any review
[2026-06-02] | wrote `ImagePicker.MediaTypeOptions.Images` in QR upload + Profile upload + SignUp — expo-image-picker SDK 54 REMOVED the enum, the accessor returns undefined and the picker throws "undefined is not a function" before opening | in expo-image-picker SDK 54+, the only supported `mediaTypes` form is the array literal `['images']` / `['videos']`. Grep the whole project for `MediaTypeOptions` and replace before shipping any new picker code
[2026-06-02] | called `CameraView.scanFromURLAsync(uri, ['qr'])` in ConnectQRScreen — expo-camera 17 exposes `scanFromURLAsync` as a module-level export, NOT a static on CameraView, so the call dereferenced undefined and surfaced as "undefined is not a function" inside a try/catch that already had a misleading fallback message | in expo-camera 17+, import `scanFromURLAsync` directly from `'expo-camera'` and call it as a function. Static-on-component-class patterns from older SDKs (`Camera.scanFromURLAsync` / `CameraView.scanFromURLAsync`) no longer exist
[2026-06-02] | UsersController.UploadImage used a relative `Path.Combine("wwwroot", "images")` — works locally because the VS run cwd happens to be the project folder, but on Azure App Service the cwd is `D:\home\site\wwwroot` and the relative path resolves differently → uploads "succeeded" then 404'd on read | for any disk write inside an ASP.NET controller, inject `IWebHostEnvironment env` and use `env.WebRootPath` (fallback `Path.Combine(env.ContentRootPath, "wwwroot")` if null). Never trust the process cwd in a hosted environment
[2026-06-02] | axios multipart upload from React Native silently surfaced as "Network Error" with no HTTP status — RN's axios adapter mishandles the multipart boundary when `Content-Type: multipart/form-data` is set explicitly (the boundary parameter gets stripped) | for multipart uploads from RN, prefer raw `fetch` with FormData and NO Content-Type header — RN sets the correct `multipart/form-data; boundary=...` automatically. If you must use axios, strip Content-Type via `transformRequest`. Also use AbortController for a 60s+ timeout — 15s is too short for cellular image uploads
[2026-06-02] | `overflow: 'hidden'` on the avatar TouchableOpacity clipped the camera edit badge that sat in its bottom-right corner — image stayed clipped to circle ✓ but badge was half-cut | when an absolute-positioned child needs to sit on/outside a clipped parent, wrap the clipped content in an INNER View (carrying `overflow: 'hidden'`) and let the badge be a sibling of that inner view inside an UNCLIPPED outer wrapper. Don't clip the parent that owns the badge
[2026-06-02] | tried to save TWDB.sql with content typed in chat — got UTF-16 LE with BOM after running `Get-Content` / `Out-File` defaults, which broke every paste-into-SSMS attempt with `Msg 102 near '.'` even on syntactically perfect SQL | when reading or writing `.sql` files exported from SSMS "Generate Scripts", expect UTF-16 LE with BOM. Read with `Get-Content -Encoding Unicode` in PowerShell. When pasting SQL from chat into SSMS fails with parse errors, bounce through Notepad first to strip rich-text/encoding contamination
[2026-06-02] | scheduleWeeklyReminder used `Notifications.cancelAllScheduledNotificationsAsync()` then scheduled a TEST notification body (`acRatio=X, level=Red`) that fired on every workout confirm — the comment said "remove when done" but it never was | TEMP TEST OVERRIDE blocks need a self-destruct: either (a) gate on `__DEV__` so release builds skip them, or (b) add a TODO with a removal date that triggers a CI lint. A debug push that ships to production looks identical to a real bug to the user
[2026-06-02] | `Coaches` row is only inserted by `UserBL.Create` when `IsCoach == true` at signup — if a user signs up as trainee then later wants to act as coach, `GET /coach/by-user/{id}` returns 404 and `myCoachId` stays null forever | when adding a flag-flip scenario (trainee → coach), also add either (a) a backfill query the user can run, or (b) a lazy-create in the BL that inserts the Coach row on first read. Don't rely on signup-time side effects for roles that can change later
[2026-06-03] | for the route-maps feature I added `android.permission.health.READ_EXERCISE_ROUTE` to app.json + AndroidManifest — that permission does NOT exist (Android only defines the privileged plural `READ_EXERCISE_ROUTES`; react-native-health-connect has no read-route permission at all). Declaring an unrecognized `android.permission.health.*` made Health Connect on Android 14+/16 DROP TrainWise from its app-permissions list entirely (worse than the rationale-intent wall: the app vanished, no permission intent fired) | NEVER declare a health permission unless it maps to a real `HealthPermission.*` constant. Exercise routes are read via the per-record CONSENT flow `requestExerciseRoute(recordId)` which needs NO manifest permission — verify a permission string exists in the library's PermissionUtils.kt before adding it. When HC discovery breaks after a manifest change, suspect a bogus/unsupported health permission first
[2026-06-03] | THE REAL "Android 16 HC wall" (open since 31-05) was a manifest regression, not an OS limitation: on Android 14+ (HC built into the OS), Health Connect populates its app-permissions list ONLY from apps that declare an `<activity-alias>` handling `android.intent.action.VIEW_PERMISSION_USAGE` + category `android.intent.category.HEALTH_PERMISSIONS` (with `android:permission="android.permission.START_VIEW_PERMISSION_USAGE"`). The legacy `androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE` alias is for Android 13 and below ONLY. An EAS/prebuild wipe on 31-05 dropped the Android-14+ alias, and the manifest was "restored" from CLAUDE.md's checklist which only listed the legacy intent — so it stayed broken. Proven via `adb shell cmd package query-activities -a android.intent.action.VIEW_PERMISSION_USAGE -c android.intent.category.HEALTH_PERMISSIONS`: CASIO/Fit/Samsung Health/Claude all resolved it, TrainWise did not | when an app is ABSENT from Health Connect's list on Android 14+ (vs present-but-denied), it's the missing VIEW_PERMISSION_USAGE/HEALTH_PERMISSIONS activity-alias — declare BOTH the legacy rationale alias AND the Android-14+ alias. Use `pm query-activities` to compare against apps that DO appear. Don't trust "App should support rationale intent, finishing!" as the whole story — that legacy-intent log is a side effect, not the root cause on Android 14+
[2026-06-07] | converted SignUpScreen WheelPicker highlight band from a translucent rgba fill to an OPAQUE palette token (Colors.cardBackgroundLight) during theming — the band sits above the numbers (zIndex 1) so the opaque fill hid the centered/selected value (blank center) | when theming an overlay that sits ON TOP of content, keep it transparent or translucent — never swap a translucent rgba for an opaque token without checking it does not cover text underneath
[2026-06-07] | the two axios clients (src/api/api.js and src/services/api.js) drifted to different LAN IPs (.119 vs .117) — login/profile worked (api.js) while coach/QR/upload/chat failed with "Network Error" (services/api.js) | BASE_URL must be identical in BOTH api files; a half-working app where only some screens get Network Error is the signature of an api.js vs services/api.js IP mismatch — check both first
[2026-06-07] | backend has nullable reference types ON, so a plain `string` prop on a DTO under `[ApiController]` is implicitly `[Required]` — text-only chat messages (imagePath:null) 400'd with "ImagePath field is required", surfaced to the user as "[object Object]" | when a backend has nullable refs enabled, any DTO field that can legitimately be omitted/null MUST be declared `string?` (or have `[Required]` deliberately) — a non-nullable `string` is auto-required by model validation. And never render an axios error body straight into an alert: branch on typeof (string passthrough vs object → .errors/.title/.message)
[2026-06-07] | "weather card not appearing" looked like a frontend bug but was a Google Cloud `403 SERVICE_DISABLED` — the Weather API wasn't enabled on the project | before debugging client code for a Google-API feature that returns nothing, curl the actual endpoint with the project key (sandbox disabled) and read the status — SERVICE_DISABLED / billing-not-linked are console-side, not code-side. Google Weather API is a SEPARATE SKU from Maps SDK (does NOT consume map-load quota) but still needs the API enabled + a billing account linked
[2026-06-07] | a manual pull-to-refresh on the Health tab did nothing because runAutoSync was gated by a 30s AUTOSYNC_THROTTLE_MS that was almost always still "fresh" after app foreground — so the actual triggerSync was skipped | a USER-INITIATED refresh must bypass time-based throttles meant for automatic/background runs. Add a `force` flag (`runAutoSync(force)`) that ignores the throttle and have the pull-to-refresh handler pass true. Throttles are for the silent path, not for explicit user intent
[2026-06-08] | MyCoachScreen assumed a single coach (HomeScreen passed coaches[0] + a GLOBAL unread badge), so a trainee linked to 2+ coaches couldn't tell which coach messaged or open the 2nd coach's chat | a one-to-many relationship rendered as a single fixed item is a silent dead end the moment a second item exists. When a screen shows "your X" and X can be plural (coaches, devices, goals), make the screen fetch the full list itself and render a selectable list with per-item state (here: per-coach unread, derived client-side from each conversation) — don't rely on the caller pre-picking item[0]
[2026-06-09] | a hardcoded Google API key `AIza...` lived in BOTH app.json AND weatherService.js and got pushed to GitHub; my pre-commit secret scan only looked for `sk-`/passwords and MISSED the Google key | before any git add/commit/push, grep staged content for `AIza` (Google) as well as `sk-`/`AKIA`/`ghp_`/`xox`/`-----BEGIN`/`Password=<value>`. Removing a key from the latest file does NOT remove it from history — the key must be ROTATED (git filter-repo --replace-text + force-push only scrubs history; rotation is the real fix). Keys belong in `.env` (gitignored); inject the native Maps key via `app.config.js` from `GOOGLE_MAPS_API_KEY`, keep app.json's apiKey an empty placeholder, never a literal
[2026-06-09] | after moving the Google key out of source the in-app weather/smart-card silently broke: weatherService reads the key at RUNTIME but the user set plain `GOOGLE_MAPS_API_KEY` — non-`EXPO_PUBLIC_` env vars are NOT inlined into the JS bundle | any key the JS runtime needs (fetch calls) MUST be `EXPO_PUBLIC_`-prefixed (inlined at build). `GOOGLE_MAPS_API_KEY` (no prefix) only feeds app.config.js → native manifest; the REST calls also need `EXPO_PUBLIC_GOOGLE_MAPS_API_KEY`. The smart card hides silently on ANY weather failure, so add a temporary visible error to surface the real reason (empty key / location denied / 403)
[2026-06-09] | a key restricted to "Android apps" (package + SHA-1) works for the native Maps SDK but 403s the Weather/AirQuality/Places REST calls (a plain fetch() carries no Android app attestation) | a single key used for BOTH the Maps SDK and JS REST APIs cannot have an Android-app application restriction — use API restrictions only, or a second app-unrestricted key for the REST calls. Confirm a key works by curling the endpoint with it (sandbox off) and reading the HTTP status
[2026-06-12] | `./gradlew assembleRelease` printed BUILD SUCCESSFUL but did NOT repackage the APK (identical date/size) because the JS-bundle task was treated as up-to-date after JS edits | RN/Expo gradle incremental builds often miss JS source changes; to force a fresh APK that includes JS edits run `./gradlew clean assembleRelease` (or `npx expo run:android` which re-bundles + re-prebuilds). Verify by the APK's timestamp/size CHANGING, not by the "BUILD SUCCESSFUL" line. The APK is always at `android/app/build/outputs/apk/release/app-release.apk` — never a manually-copied location. gradlew alone does NOT re-prebuild, so a rotated native Maps key won't reach the manifest without `expo run:android`/prebuild. Local-LAN APKs have the PC IP baked in → only work for devices on the same WiFi with the backend running
[2026-06-13] | for the ML coach feature I wrote the Python service as plain-Python modules; the user (ML/Data-Science course) expected code in the Course-ML notebook idiom — full pandas/numpy/seaborn/matplotlib/sklearn | the gradeable ML artifact must be a course-style NOTEBOOK (markdown sections, seaborn EDA, train/test split, MAE/MSE/RMSE + Accuracy/F1 + ROC-AUC, residual analysis, model-comparison bars, pickle export). seaborn/matplotlib are plotting libs → they live in the notebook, NOT in a non-plotting Flask service; keep the service's analysis modules pandas/numpy/sklearn-idiomatic (vectorized, not raw loops)
[2026-06-13] | multiclass `roc_auc_score(y_bin, proba)` returned 0.233 (worse than random) for a 0.999-accuracy model because `label_binarize(classes=[custom order])` columns didn't match `predict_proba` columns (ordered by `clf.classes_`, alphabetical) | when computing multiclass ROC/AUC, reorder proba to the label order first: `proba = clf.predict_proba(X)[:, [list(clf.classes_).index(c) for c in classes]]`, then both per-class `roc_curve` and macro `roc_auc_score` line up. A sub-0.5 AUC on a high-accuracy model is the signature of swapped class columns
[2026-06-13] | the forecast's per-week AC projection divided a rising projected acute load by today's FROZEN chronic, producing impossible ratios (e.g. 4.0) | project load forward day-by-day and RECOMPUTE chronic over the simulated series (chronic adapts as "they keep training like this", so AC converges ~1.0). Also: with <2 completed month-weeks, fit nothing — project the current trailing-7-day acute ("recent pace") so a heavy ongoing (incomplete) week isn't ignored just because week 1 was light
[2026-06-16] | the in-app Google Map rendered blank (beige grid + logo, no tiles) after a rebuild even though the Cloud key had App-restrictions=None, Maps SDK enabled, billing active, and the metrics showed 0 Maps-SDK requests | the prebuilt `android/app/src/main/AndroidManifest.xml` had the OLD Maps key hardcoded in `com.google.android.geo.API_KEY` while `.env` had the NEW key. Local gradle builds read the key from the NATIVE MANIFEST, not `.env`/app.config.js — so updating `.env` never reaches the APK's map. (Weather/AirQuality kept working because they read the key from the JS bundle, which DOES evaluate `.env` — a split that proves the key itself is valid.) `android/` is gitignored, so the surgical fix is to edit manifest line ~35 to the `.env` value (no key gets committed); the "proper" fix is `expo prebuild`, but that can clobber hand-applied native edits (Health Connect aliases) so avoid it on this project. Diagnose via the Maps Platform Metrics page: real key reaching the project = requests appear; 0 requests + valid key elsewhere = wrong key baked into the manifest
[2026-06-18] | adding the sign-up reCAPTCHA, `import { WebView } from 'react-native-webview'` made `./gradlew assembleRelease` fail at `createBundleReleaseJsAndAssets` with "Unable to resolve module react-native-webview" — even though it was listed in package.json | a dependency being in package.json does NOT mean it's in node_modules. react-native-webview was declared but never `npm install`ed; nothing imported it before so no build caught it. Before importing ANY (especially native) module, run `npx expo install <pkg>` and confirm `node_modules/<pkg>/android` exists. expo install also picks the SDK-compatible version
[2026-06-18] | `./gradlew assembleRelease` failed mid-build with "FATAL ERROR: Ineffective mark-compacts near heap limit — JavaScript heap out of memory" in the JS-bundle step (Node default ~4GB heap), and a flaky retry only succeeded by reusing a stale bundle | build release with `NODE_OPTIONS=--max-old-space-size=8192 ./gradlew assembleRelease --no-parallel`. The 8192 is BUILD-TIME RAM on the PC (Node bundler), NOT app size — the APK stays ~125MB; users panic that "the app will be 8GB", reassure them. `--no-parallel` caps peak memory so concurrent native compiles don't OOM. To make it permanent, a gradle.properties / NODE_OPTIONS env tweak (offered, not yet applied)
[2026-06-18] | Health Connect workout times displayed 3 hours early (12:00 recorded showed as 09:00) | the C# backend returns SQL `datetime` columns with NO timezone designator (`"2026-06-15T10:42:28.313"`, no `Z`/offset); the stored instants are UTC (app posts `Date.toISOString()`), but `new Date(str)` in RN parses a zone-less string as DEVICE-LOCAL (Jerusalem +3) → drift. Fix: a `parseServerDate()` helper ([utils/serverDate.js](../TrainWiseExpo/src/utils/serverDate.js)) that appends `Z` when the string carries no zone, used in every workout-time display. Also: a Hermes-bytecode APK stores emoji-containing strings as UTF-16, so ASCII `grep` for them returns 0 even when present — verify shipped features with pure-ASCII strings instead
