# =================== Visual Studio / .NET ===================
.vs/
[Bb]in/
[Oo]bj/
*.user
*.suo
*.userosscache
*.sln.docstates
*.cache
*.vspscc

# =================== Node / React Native / Expo ===================
node_modules/
.expo/
.expo-shared/
dist/
web-build/
.metro-health-check*

# =================== Android (RN native) ===================
android/build/
android/.gradle/
android/.cxx/
android/app/build/
android/app/.cxx/
android/local.properties
android/.idea/
*.keystore
!debug.keystore

# =================== iOS (RN native) ===================
ios/build/
ios/Pods/
ios/Podfile.lock
ios/*.xcworkspace/xcuserdata/
ios/*.xcodeproj/xcuserdata/
ios/*.xcodeproj/project.xcworkspace/xcuserdata/

# =================== Build outputs ===================
build/
*.jsbundle
*.apk
*.aab
*.ipa

# =================== Logs ===================
npm-debug.log*
yarn-error.log
yarn-debug.log*
*.log

# =================== OS files ===================
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# =================== IDE ===================
.idea/
.vscode/
*.iml
.gradle
local.properties

# =================== Python (PDF generator scripts) ===================
__pycache__/
*.pyc
venv/
.env

# =================== Claude / scratch ===================
.claude/

# =================== Temporary scratch projects ===================
my-app/

# =================== Runtime-uploaded content (not source) ===================
# Profile / chat images saved by the API at runtime. Keep the folder, drop files.
TrainWise/TrainWise/wwwroot/images/*
!TrainWise/TrainWise/wwwroot/images/.gitkeep

# =================== Azure publish profiles (personal deploy config) ===================
TrainWise/TrainWise/Properties/PublishProfiles/
TrainWise/TrainWise/Properties/ServiceDependencies/

# =================== Stray root EAS ignore (EAS uses TrainWiseExpo/.easignore) ===================
/.easignore

# =================== Archives / project zips (not source) ===================
*.zip

# =================== Not project source / sensitive (do NOT commit) ===================
# Unrelated ML-course homework (PDFs, CSVs, lesson notebooks) — not TrainWise source
Python Course ML/
# Full dump of the live DB — contains real user PII + credentials
sql/full_data_insert.sql
sql/export_all_data.sql
# Local design-experiment backups — git history is the real backup
tasks/design_backups/
# Trained model binaries — regenerated by the notebook (ml/models stays empty in git)
ml/models/*.pkl
