// Google OAuth - Web Client ID only (public; safe to ship). Used as `webClientId`
// for @react-native-google-signin so the native sign-in returns an idToken the
// backend verifies. From Firebase project trainwise-ef6aa (the project YOU own;
// the old 184677449878 client belonged to an inaccessible project). The client
// SECRET must never live in app code — it belongs on the server.
export const GOOGLE_WEB_CLIENT_ID =
  '388904145749-9fg97kftfjbuaarj0oma1poi88ok2h2j.apps.googleusercontent.com';
